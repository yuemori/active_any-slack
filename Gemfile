# frozen_string_literal: true

source 'https://rubygems.org'

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

gem 'active_any', path: '../active_any'
# Specify your gem's dependencies in active_any-slack.gemspec
gemspec
